# Development nginx config - HTTP only, no SSL required
server {
    listen 80;
    listen [::]:80;  # IPv6 support
    server_name localhost 127.0.0.1;

    # robots.txt - serve directly from nginx
    location = /robots.txt {
        alias /app/staticfiles/robots.txt;
        access_log off;
        log_not_found off;
        expires 1d;
        add_header Cache-Control "public";
    }

    # Google Search Console verification files
    location ~ ^/google[a-z0-9]+\.html$ {
        alias /app/staticfiles/$uri;
        access_log off;
        log_not_found off;
        expires 1d;
        add_header Cache-Control "public";
    }

    # Static files
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Media files
    location /media/ {
        alias /app/media/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    # Django application
    location / {
        proxy_pass http://ecommerce_app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $server_name;
        proxy_redirect off;
        
        # Timeout settings
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        # Buffer settings
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }
}

